version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - chmod +x ./gradlew

  build:
    commands:
      - ./gradlew clean bootJar -x test
      - cp build/libs/*.jar app.jar

artifacts:
  files:
    - app.jar
    - appspec.yml
    - scripts/**/*